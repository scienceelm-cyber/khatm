# راهنمای کاملاً قدم‌به‌قدم نصب «ختم جمعی» روی هاست شخصی

این راهنما برای کسی نوشته شده که قبلاً سایت یا اپ منتشر نکرده است. دستورها را دقیقاً به ترتیب اجرا کنید و هیچ مرحله‌ای را رد نکنید.

## اول بدانید چه چیزی نصب می‌کنید

سایت «ختم جمعی» برای هماهنگ‌کردن آیات، صلوات‌ها، نیت‌ها و هدیه‌های معنوی به یک پایگاه دادهٔ زنده نیاز دارد. بنابراین:

- هاست معمولی که فقط فایل HTML/PHP یا cPanel دارد، به‌تنهایی کافی نیست.
- روش رسمی و آزمایش‌شدهٔ این نسخه، **Cloudflare Workers + Cloudflare D1** است.
- Cloudflare اتصال HTTPS را فراهم می‌کند و اپ اندروید فقط به آدرس HTTPS وصل می‌شود.
- سایت و اپ باید هر دو به **یک آدرس سرور** متصل باشند تا شمارنده‌ها و نیت‌ها واقعاً یکپارچه بمانند.

## چیزهایی که لازم دارید

1. یک حساب Cloudflare.
2. یک رایانه ویندوز، macOS یا Linux.
3. Node.js نسخهٔ 22.13 یا جدیدتر.
4. فایل ZIP سورس قفل‌شدهٔ ختم که همراه این راهنما تحویل شده است.
5. برای ساخت APK اختصاصی: Android Studio و فایل کلید امضای خصوصی.

## بخش اول — نصب سایت روی Cloudflare

### قدم ۱: فایل سورس را باز کنید

1. فایل ZIP سورس را در یک پوشهٔ تازه Extract کنید.
2. بهتر است نام مسیر انگلیسی و کوتاه باشد؛ برای مثال در ویندوز:

```text
C:\khatm
```

3. وارد پوشه شوید. باید فایل‌های `package.json`، `wrangler.selfhost.example.jsonc` و پوشهٔ `drizzle` را ببینید.

### قدم ۲: Node.js را نصب کنید

1. سایت رسمی Node.js را باز کنید.
2. نسخهٔ LTS را نصب کنید؛ نسخه باید حداقل 22.13 باشد.
3. بعد از نصب، پنجرهٔ PowerShell یا Terminal را ببندید و دوباره باز کنید.
4. این دستور را اجرا کنید:

```bash
node --version
```

اگر عددی مانند `v22.13.0` یا بزرگ‌تر دیدید، درست است.

### قدم ۳: Terminal را در پوشهٔ پروژه باز کنید

در ویندوز، پوشهٔ پروژه را در File Explorer باز کنید، نوار آدرس را انتخاب کنید، `powershell` بنویسید و Enter بزنید.

برای اطمینان، این دستور را اجرا کنید:

```bash
dir
```

باید `package.json` را در فهرست ببینید.

### قدم ۴: ابزارهای پروژه را نصب کنید

این دستور را دقیقاً اجرا کنید:

```bash
npm ci
```

تا وقتی دوباره خط فرمان ظاهر نشده، پنجره را نبندید. پیام‌های معمولی `npm` خطا نیستند؛ اگر پایان کار عبارت `npm error` دیدید، بخش رفع اشکال پایین راهنما را بخوانید.

### قدم ۵: وارد حساب Cloudflare شوید

این دستور را اجرا کنید:

```bash
npx wrangler login
```

1. مرورگر باز می‌شود.
2. وارد حساب Cloudflare شوید.
3. اجازهٔ Wrangler را تأیید کنید.
4. به Terminal برگردید.

برای کنترل ورود:

```bash
npx wrangler whoami
```

باید نام حساب خودتان را ببینید.

### قدم ۶: پایگاه داده را بسازید

این دستور را اجرا کنید:

```bash
npx wrangler d1 create khatm-db
```

Cloudflare چند خط نمایش می‌دهد. خطی شبیه این مهم است:

```text
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

فقط مقدار بین علامت نقل‌قول را کامل کپی کنید. این شناسه رمز نیست، اما باید دقیق باشد.

### قدم ۷: فایل تنظیمات را بسازید

این دستور را اجرا کنید:

```bash
npm run selfhost:prepare
```

حالا فایل تازه‌ای با نام `wrangler.jsonc` ساخته شده است.

1. فایل `wrangler.jsonc` را با Notepad یا VS Code باز کنید.
2. عبارت `PASTE_DATABASE_ID_HERE` را پیدا کنید.
3. آن را با `database_id` مرحله قبل جایگزین کنید.
4. فایل را Save کنید.
5. مقدار `binding` را تغییر ندهید؛ باید دقیقاً `DB` بماند.
6. مقدار `database_name` باید `khatm-db` بماند، مگر اینکه هنگام ساخت دیتابیس نام دیگری داده باشید.

نمونهٔ صحیح:

```json
"database_id": "12345678-1234-1234-1234-123456789abc"
```

### قدم ۸: تنظیمات را بررسی کنید

```bash
npm run selfhost:check
```

باید پیام `بررسی تنظیمات موفق بود` را ببینید. اگر خطا دیدید، متن خطا دقیقاً می‌گوید کدام مورد ناقص است.

### قدم ۹: جدول‌های دیتابیس را ایجاد کنید

```bash
npx wrangler d1 migrations apply khatm-db --remote
```

اگر پرسید ادامه بدهد، حرف `y` را وارد و Enter بزنید. باید همهٔ migrationهای پوشهٔ `drizzle` با موفقیت ثبت شوند.

این مرحله نیت‌ها، رزرو آیات، شمارنده صلوات و شمارنده هدیه‌های معنوی را برای سرور شما آماده می‌کند.

### قدم ۱۰: رمز مدیریت نیت‌ها را ثبت کنید

یک رمز تازه، طولانی و مخصوص سرور خودتان انتخاب کنید. حداقل ۲۰ کاراکتر و ترکیبی از حروف بزرگ، کوچک و عدد باشد.

این دستور را اجرا کنید:

```bash
npx wrangler secret put ADMIN_TOKEN
```

1. Wrangler از شما مقدار Secret می‌خواهد.
2. رمز تازه را Paste کنید و Enter بزنید.
3. هنگام تایپ ممکن است چیزی روی صفحه دیده نشود؛ طبیعی است.
4. رمز را داخل `wrangler.jsonc`، GitHub یا سورس ننویسید.

رمز سرور اصلی که همراه تحویل خصوصی داده شده، برای سرور اصلی است. برای هاست جدید بهتر است حتماً رمز جداگانه بسازید.

### قدم ۱۱: سایت را بسازید و منتشر کنید

```bash
npm run selfhost:deploy
```

این دستور ابتدا نسخهٔ نهایی را می‌سازد و سپس آن را منتشر می‌کند. در پایان، آدرسی شبیه زیر نمایش داده می‌شود:

```text
https://khatm-selfhost.YOUR-SUBDOMAIN.workers.dev
```

این آدرس را کامل ذخیره کنید. از این به بعد «آدرس سرور شما» است.

### قدم ۱۲: سلامت سرور را کنترل کنید

آدرس زیر را در مرورگر باز کنید؛ به‌جای دامنه نمونه، دامنه خودتان را بگذارید:

```text
https://khatm-selfhost.YOUR-SUBDOMAIN.workers.dev/api/health
```

باید پاسخی شامل `"ok":true` ببینید. سپس خود آدرس اصلی سایت را باز کنید و یک نیت، آیه و بخش هدیه معنوی را کنترل کنید.

### قدم ۱۳: دامنه شخصی وصل کنید (اختیاری)

1. وارد داشبورد Cloudflare شوید.
2. به بخش Workers & Pages بروید.
3. Worker با نام `khatm-selfhost` را باز کنید.
4. وارد Settings و سپس Domains & Routes شوید.
5. Add را بزنید و Custom Domain را انتخاب کنید.
6. دامنه یا زیردامنه‌ای مانند `khatm.example.com` را وارد کنید.
7. پس از فعال‌شدن HTTPS، دوباره `/api/health` را با دامنه جدید باز کنید.

اگر دامنه شما در Cloudflare مدیریت نمی‌شود، اول باید DNS آن را مطابق راهنمای خود Cloudflare منتقل یا تنظیم کنید.

## بخش دوم — ورود به مدیریت و تغییر نیت‌ها

### در وب‌سایت

1. سایت را باز کنید.
2. بالای صفحه دکمهٔ **مدیریت نیت‌ها** یا آیکن چرخ‌دنده را بزنید.
3. رمز `ADMIN_TOKEN` همان سرور را وارد کنید.
4. دکمهٔ ورود/تأیید را بزنید.

پس از ورود:

- برای نیت جدید، **افزودن نیت** را بزنید، عنوان و توضیح را وارد کنید، هدف صلوات را تعیین و ذخیره کنید.
- برای اصلاح، کنار همان نیت **ویرایش** را بزنید و ذخیره کنید.
- برای برداشتن نیت از فهرست عمومی، **بایگانی** را بزنید. اطلاعات قبلی حذف دائمی نمی‌شود.
- حذف یا ویرایش نیت، شمارندهٔ نیت‌های دیگر را تغییر نمی‌دهد.

### در اپ اندروید

1. اپ را باز کنید.
2. بالای صفحه آیکن سپر/مدیریت با عنوان **مدیریت نیت‌ها** را بزنید.
3. رمز مدیریت همان سروری را وارد کنید که اپ به آن وصل است.
4. افزودن، ویرایش یا بایگانی را انجام دهید.

رمز مدیریت در اپ ذخیره دائمی نمی‌شود و داخل APK قرار ندارد. هر بار که بخش مدیریت به آن نیاز داشته باشد، خودتان واردش می‌کنید.

## بخش سوم — نصب APK کامل آماده

فایل `Khatm-2.1.0-FULL.apk` یک برنامهٔ کامل است، نه فایل افزایشی و نه Patch. می‌توانید آن را روی گوشی‌ای که نسخه قبلی از آن حذف شده، مستقیماً نصب کنید.

1. فایل APK را به گوشی منتقل یا مستقیم دانلود کنید.
2. روی فایل بزنید.
3. اگر Android اجازه نداد، فقط برای همان مرورگر یا Files گزینهٔ **Install unknown apps** را فعال کنید.
4. Install را بزنید.
5. بعد از نصب، اجازه نصب ناشناس را دوباره خاموش کنید.

APK آماده به سرور اصلی زیر وصل است:

```text
https://khatm.imangpt1996.chatgpt.site
```

## بخش چهارم — ساخت APK کامل برای هاست خودتان

اگر می‌خواهید اپ به Worker یا دامنه شخصی شما وصل شود، باید یک APK اختصاصی بسازید.

### قدم ۱: Android Studio را نصب کنید

1. Android Studio را از سایت رسمی Android نصب کنید.
2. هنگام نصب، Android SDK و ابزارهای پیشنهادی را نیز انتخاب کنید.
3. Android Studio را اجرا کنید.

### قدم ۲: پوشهٔ اندروید را باز کنید

1. در صفحه شروع Android Studio گزینهٔ Open را بزنید.
2. فقط پوشهٔ `android` داخل سورس ختم را انتخاب کنید.
3. صبر کنید پیام Gradle Sync تمام شود.
4. اگر نصب SDK 37 پیشنهاد شد، Install را بزنید.

### قدم ۳: آدرس سرور خودتان را ثبت کنید

در پوشهٔ `android` فایلی به نام `khatm.local.properties.example` وجود دارد.

1. از آن یک Copy بگیرید.
2. نام Copy را دقیقاً `khatm.local.properties` بگذارید.
3. فایل جدید را باز کنید.
4. مقدار نمونه را با آدرس HTTPS خودتان عوض کنید؛ انتهای آدرس `/` نگذارید.

نمونه:

```properties
KHATM_BASE_URL=https://khatm.example.com
```

این فایل عمداً توسط Git نادیده گرفته می‌شود. هیچ‌وقت `ADMIN_TOKEN` را در این فایل قرار ندهید.

### قدم ۴: نسخه آزمایشی را بسازید

در Terminal داخلی Android Studio:

ویندوز:

```powershell
gradlew.bat testDebugUnitTest lintDebug assembleDebug
```

macOS یا Linux:

```bash
./gradlew testDebugUnitTest lintDebug assembleDebug
```

APK آزمایشی در این مسیر ساخته می‌شود:

```text
android/app/build/outputs/apk/debug/app-debug.apk
```

آن را روی گوشی نصب کنید و مطمئن شوید نیت‌ها و آمار همان سایت خودتان را نشان می‌دهد.

### قدم ۵: نسخه نهایی امضاشده را بسازید

1. در Android Studio از منوی Build گزینهٔ **Generate Signed App Bundle or APK** را بزنید.
2. گزینهٔ APK را انتخاب کنید و Next بزنید.
3. اگر فایل keystore خصوصی این پروژه را دارید، Choose existing را بزنید و آن را انتخاب کنید.
4. Key alias و رمزهای فایل بازیابی خصوصی را وارد کنید.
5. Build variant را `release` بگذارید.
6. هر دو Signature Version یعنی V1 و V2 را فعال کنید.
7. Finish را بزنید.

فایل خروجی معمولاً اینجا قرار می‌گیرد:

```text
android/app/release/app-release.apk
```

### قانون بسیار مهم امضا

- برای هر به‌روزرسانی آینده باید از **همان keystore و همان alias** استفاده کنید.
- اگر keystore را گم کنید، نمی‌توانید روی نسخه نصب‌شدهٔ قبلی به‌روزرسانی مستقیم بدهید.
- keystore و رمز آن را در ایمیل عمومی، GitHub، سورس ZIP یا هاست آپلود نکنید.
- یک نسخه روی حافظه آفلاین امن و یک نسخه پشتیبان رمزگذاری‌شده نگه دارید.

## به‌روزرسانی سایت در آینده

1. از دیتابیس و keystore نسخه پشتیبان داشته باشید.
2. سورس نسخه جدید را باز کنید.
3. فایل `wrangler.jsonc` خودتان را منتقل کنید یا دوباره بسازید.
4. این دستورها را به ترتیب اجرا کنید:

```bash
npm ci
npx wrangler login
npx wrangler d1 migrations apply khatm-db --remote
npm run selfhost:deploy
```

رمز `ADMIN_TOKEN` در Cloudflare باقی می‌ماند و لازم نیست داخل سورس منتقل شود.

## رفع اشکال‌های معمول

### `node` یا `npm` شناخته نمی‌شود

Node.js نصب نشده یا Terminal پس از نصب دوباره باز نشده است. Node.js LTS را نصب و پنجره را دوباره باز کنید.

### `Not logged in`

دوباره اجرا کنید:

```bash
npx wrangler login
```

### خطای `PASTE_DATABASE_ID_HERE`

فایل `wrangler.jsonc` را باز کنید و شناسه واقعی دیتابیس مرحله ۶ را جایگزین کنید.

### خطای جدول یا `no such table`

Migrationها اجرا نشده‌اند. این دستور را اجرا کنید:

```bash
npx wrangler d1 migrations apply khatm-db --remote
```

### رمز مدیریت قبول نمی‌شود

رمز مربوط به همان Worker را وارد کنید. برای تعریف دوباره رمز:

```bash
npx wrangler secret put ADMIN_TOKEN
```

بعد Worker را دوباره منتشر کنید:

```bash
npm run selfhost:deploy
```

### اپ اطلاعات سایت جدید را نشان نمی‌دهد

فایل `android/khatm.local.properties` را کنترل کنید، سپس اپ را دوباره Build و نصب کنید. APK آمادهٔ عمومی عمداً به سرور اصلی وصل است.

### Android جلوی نصب APK را می‌گیرد

فقط APK امضاشده را نصب کنید. اگر پیام Play Protect یا ناسازگاری امضا دیدید، فایل را نصب نکنید و ابتدا امضا و SHA-256 را با فایل تحویل کنترل کنید.

## چک‌لیست نهایی

- [ ] `/api/health` پاسخ `ok: true` می‌دهد.
- [ ] سایت فقط با HTTPS باز می‌شود.
- [ ] `ADMIN_TOKEN` به‌صورت Secret ثبت شده و داخل سورس نیست.
- [ ] افزودن و ویرایش نیت در وب و اپ یک نتیجه نشان می‌دهد.
- [ ] ثبت آیه، صلوات و هدیه معنوی در هر دو محیط همگام است.
- [ ] APK با keystore اصلی امضا شده است.
- [ ] keystore و رمزها در محل خصوصی و امن نگه‌داری می‌شوند.
